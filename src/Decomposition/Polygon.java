package Decomposition;

public class Polygon {
        public static void PolygonSquare(double a){
            double s = ((3*Math.sqrt(3))/2) * Math.pow(a,2);
            System.out.println(s);
        }
}
